'use strict';
var expect = require('chai').expect;

describe('tests', function () {
    before(function () {
        this.users = require('../lib');
    });

    it('ah', function(done) {
        done();
    });
});